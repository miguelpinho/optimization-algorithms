function [ distance_to_section ] = task5( point, center, radius )
%TASK5 Summary of this function goes here
%   Detailed explanation goes here
if square_pos( norm(point - center ) > radius
    return square_pos( norm(point - center ) - radius;
end
elseif square_pos( norm(point - center ) <= radius
    return 0;
end

end

